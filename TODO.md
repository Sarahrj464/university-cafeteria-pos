# TODO

- [ ] Restore orange button styling everywhere by hardening `frontend/src/components/ui/Button.jsx` accent variant (ensure class is not overridden by any global styles).
- [ ] Verify login/sign-in buttons visibility issue by checking Auth page render conditions and button tab styles.
- [ ] Ensure POS cashier Pay button remains orange (variant="accent"), and add regression check by rebuilding frontend.

